# k8s/02-secret.yaml
# Secret for sensitive configuration (passwords, keys)
# NOTE: In production, use proper secret management (e.g., Sealed Secrets, Vault)
apiVersion: v1
kind: Secret
metadata:
  name: fibonacci-secret
  namespace: fibonacci
type: Opaque
stringData:
  DB_PASSWORD: "password"
  SECRET_KEY: "dev-secret-key-change-in-production-k8s"
  COINGECKO_API_KEY: ""
